<%= form_for([@customer, @sub_time.task, @sub_time]) do |f| %>
  <% if @sub_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_time.errors.count, "error") %> prohibited this sub_time from being saved:</h2>

      <ul>
      <% @sub_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br />
    <%= f.text_field :start, :id => 'test-1' %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= f.datetime_select :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
